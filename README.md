# My termux config

## Setup

1. Expose ports `termux-setup-storage`
1. Install ssh `pkg install openssh`
1. Generate ssh key `ssh-keygen`
1. Export the public ssh key and add it where needed
1. Clone git repo `git clone git@github.com:carlba/termux.git`
1. Reload settings `termux-reload-settings`
